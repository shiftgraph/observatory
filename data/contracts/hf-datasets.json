{
  "schema_version": 1,
  "id": "hf-datasets",
  "provider": "huggingface",
  "category": "model-hub",
  "method": "GET",
  "url": "https://huggingface.co/api/datasets?limit=3",
  "host": "huggingface.co",
  "path": "/api/datasets",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 15,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "array",
    "element": {
      "type": "object",
      "keys": {
        "_id": {
          "type": "string"
        },
        "author": {
          "type": "string"
        },
        "createdAt": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "disabled": {
          "type": "boolean"
        },
        "downloads": {
          "type": "number"
        },
        "gated": {
          "type": "boolean"
        },
        "id": {
          "type": "string"
        },
        "key": {
          "type": "string"
        },
        "lastModified": {
          "type": "string"
        },
        "likes": {
          "type": "number"
        },
        "private": {
          "type": "boolean"
        },
        "sha": {
          "type": "string"
        },
        "tags": {
          "type": "array",
          "element": {
            "type": "string"
          }
        },
        "trendingScore": {
          "type": "number"
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://huggingface.co/api/datasets?limit=3\n// observations  : 34\n// observed      : 2026-07-26 to 2026-08-03\n// fields        : 15 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://huggingface.co/api/datasets?limit=3\nexport interface HfDatasets Array<{\n  _id: string;\n  author: string;\n  createdAt: string;\n  description: string;\n  disabled: boolean;\n  downloads: number;\n  gated: boolean;\n  id: string;\n  key: string;\n  lastModified: string;\n  likes: number;\n  private: boolean;\n  sha: string;\n  tags: Array<string>;\n  trendingScore: number;\n}>\n\nimport { z } from \"zod\";\n\nexport const HfDatasetsSchema = z.array(z.object({\n  _id: z.string(),\n  author: z.string(),\n  createdAt: z.string(),\n  description: z.string(),\n  disabled: z.boolean(),\n  downloads: z.number(),\n  gated: z.boolean(),\n  id: z.string(),\n  key: z.string(),\n  lastModified: z.string(),\n  likes: z.number(),\n  private: z.boolean(),\n  sha: z.string(),\n  tags: z.array(z.string()),\n  trendingScore: z.number(),\n}));\n"
}
