{
  "schema_version": 1,
  "id": "github-topics",
  "provider": "github",
  "category": "dev-platform",
  "method": "GET",
  "url": "https://api.github.com/repos/facebook/react/topics",
  "host": "api.github.com",
  "path": "/repos/facebook/react/topics",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 1,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "names": {
        "type": "array",
        "element": {
          "type": "string"
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.github.com/repos/facebook/react/topics\n// observations  : 18\n// observed      : 2026-07-26 to 2026-07-30\n// fields        : 1 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.github.com/repos/facebook/react/topics\nexport interface GithubTopics {\n  names: Array<string>;\n}\n\nimport { z } from \"zod\";\n\nexport const GithubTopicsSchema = z.object({\n  names: z.array(z.string()),\n});\n"
}
