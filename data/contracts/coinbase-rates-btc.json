{
  "schema_version": 1,
  "id": "coinbase-rates-btc",
  "provider": "coinbase",
  "category": "crypto",
  "method": "GET",
  "url": "https://api.coinbase.com/v2/exchange-rates?currency=BTC",
  "host": "api.coinbase.com",
  "path": "/v2/exchange-rates",
  "observed_from": "2026-07-24",
  "confidence": "strong",
  "fields": 4,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "data": {
        "type": "object",
        "keys": {
          "currency": {
            "type": "string"
          },
          "rates": {
            "type": "object",
            "keys": {
              "{key}": {
                "type": "string"
              }
            }
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.coinbase.com/v2/exchange-rates?currency=BTC\n// observations  : 58\n// observed      : 2026-07-24 to 2026-08-08\n// fields        : 4 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.coinbase.com/v2/exchange-rates?currency=BTC\nexport interface CoinbaseRatesBtc {\n  data: {\n    currency: string;\n    rates: Record<string, string>;\n  };\n}\n\nimport { z } from \"zod\";\n\nexport const CoinbaseRatesBtcSchema = z.object({\n  data: z.object({\n    currency: z.string(),\n    rates: z.record(z.string()),\n  }),\n});\n"
}
