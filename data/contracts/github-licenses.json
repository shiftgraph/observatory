{
  "schema_version": 1,
  "id": "github-licenses",
  "provider": "github",
  "category": "dev-platform",
  "method": "GET",
  "url": "https://api.github.com/licenses",
  "host": "api.github.com",
  "path": "/licenses",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 5,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "array",
    "element": {
      "type": "object",
      "keys": {
        "key": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "node_id": {
          "type": "string"
        },
        "spdx_id": {
          "type": "string"
        },
        "url": {
          "type": "string"
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.github.com/licenses\n// observations  : 54\n// observed      : 2026-07-26 to 2026-08-09\n// fields        : 5 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.github.com/licenses\nexport interface GithubLicenses Array<{\n  key: string;\n  name: string;\n  node_id: string;\n  spdx_id: string;\n  url: string;\n}>\n\nimport { z } from \"zod\";\n\nexport const GithubLicensesSchema = z.array(z.object({\n  key: z.string(),\n  name: z.string(),\n  node_id: z.string(),\n  spdx_id: z.string(),\n  url: z.string(),\n}));\n"
}
