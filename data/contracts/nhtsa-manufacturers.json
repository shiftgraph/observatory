{
  "schema_version": 1,
  "id": "nhtsa-manufacturers",
  "provider": "nhtsa",
  "category": "reference",
  "method": "GET",
  "url": "https://vpic.nhtsa.dot.gov/api/vehicles/getallmanufacturers?format=json&page=1",
  "host": "vpic.nhtsa.dot.gov",
  "path": "/api/vehicles/getallmanufacturers",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 11,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not",
    "typed null and never seen holding a value: $.SearchCriteria"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "Count": {
        "type": "number"
      },
      "Message": {
        "type": "string"
      },
      "Results": {
        "type": "array",
        "element": {
          "type": "object",
          "keys": {
            "Country": {
              "type": "string"
            },
            "Mfr_CommonName": {
              "type": "string"
            },
            "Mfr_ID": {
              "type": "number"
            },
            "Mfr_Name": {
              "type": "string"
            },
            "VehicleTypes": {
              "type": "array",
              "element": {
                "type": "object",
                "keys": {
                  "IsPrimary": {
                    "type": "boolean"
                  },
                  "Name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      },
      "SearchCriteria": {
        "type": "null"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://vpic.nhtsa.dot.gov/api/vehicles/getallmanufacturers?format=json&page=1\n// observations  : 118\n// observed      : 2026-07-26 to 2026-08-24\n// fields        : 11 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n// NOTE: typed null and never seen holding a value: $.SearchCriteria\n// NOTE: 1 field was observed ONLY as null, so it is typed `null` and cannot hold a value. That is what was seen, not necessarily the contract: widen to `T | null` if you know the type. $.SearchCriteria\n//\n// Regenerate: npx @shiftgraph/generate https://vpic.nhtsa.dot.gov/api/vehicles/getallmanufacturers?format=json&page=1\nexport interface NhtsaManufacturers {\n  Count: number;\n  Message: string;\n  Results: Array<{\n    Country: string;\n    Mfr_CommonName: string;\n    Mfr_ID: number;\n    Mfr_Name: string;\n    VehicleTypes: Array<{\n      IsPrimary: boolean;\n      Name: string;\n    }>;\n  }>;\n  SearchCriteria: null;\n}\n\nimport { z } from \"zod\";\n\nexport const NhtsaManufacturersSchema = z.object({\n  Count: z.number(),\n  Message: z.string(),\n  Results: z.array(z.object({\n    Country: z.string(),\n    Mfr_CommonName: z.string(),\n    Mfr_ID: z.number(),\n    Mfr_Name: z.string(),\n    VehicleTypes: z.array(z.object({\n      IsPrimary: z.boolean(),\n      Name: z.string(),\n    })),\n  })),\n  SearchCriteria: z.null(),\n});\n"
}
