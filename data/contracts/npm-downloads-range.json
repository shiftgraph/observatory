{
  "schema_version": 1,
  "id": "npm-downloads-range",
  "provider": "npm",
  "category": "dev-registry",
  "method": "GET",
  "url": "https://api.npmjs.org/downloads/range/last-week/express",
  "host": "api.npmjs.org",
  "path": "/downloads/range/last-week/express",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 6,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "downloads": {
        "type": "array",
        "element": {
          "type": "object",
          "keys": {
            "day": {
              "type": "string"
            },
            "downloads": {
              "type": "number"
            }
          }
        }
      },
      "end": {
        "type": "string"
      },
      "package": {
        "type": "string"
      },
      "start": {
        "type": "string"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.npmjs.org/downloads/range/last-week/express\n// observations  : 15\n// observed      : 2026-07-26 to 2026-07-29\n// fields        : 6 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.npmjs.org/downloads/range/last-week/express\nexport interface NpmDownloadsRange {\n  downloads: Array<{\n    day: string;\n    downloads: number;\n  }>;\n  end: string;\n  package: string;\n  start: string;\n}\n\nimport { z } from \"zod\";\n\nexport const NpmDownloadsRangeSchema = z.object({\n  downloads: z.array(z.object({\n    day: z.string(),\n    downloads: z.number(),\n  })),\n  end: z.string(),\n  package: z.string(),\n  start: z.string(),\n});\n"
}
