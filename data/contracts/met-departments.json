{
  "schema_version": 1,
  "id": "met-departments",
  "provider": "metmuseum",
  "category": "content",
  "method": "GET",
  "url": "https://collectionapi.metmuseum.org/public/collection/v1/departments",
  "host": "collectionapi.metmuseum.org",
  "path": "/public/collection/v1/departments",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 3,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "departments": {
        "type": "array",
        "element": {
          "type": "object",
          "keys": {
            "departmentId": {
              "type": "number"
            },
            "displayName": {
              "type": "string"
            }
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://collectionapi.metmuseum.org/public/collection/v1/departments\n// observations  : 64\n// observed      : 2026-07-26 to 2026-08-10\n// fields        : 3 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://collectionapi.metmuseum.org/public/collection/v1/departments\nexport interface MetDepartments {\n  departments: Array<{\n    departmentId: number;\n    displayName: string;\n  }>;\n}\n\nimport { z } from \"zod\";\n\nexport const MetDepartmentsSchema = z.object({\n  departments: z.array(z.object({\n    departmentId: z.number(),\n    displayName: z.string(),\n  })),\n});\n"
}
