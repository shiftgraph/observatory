{
  "schema_version": 1,
  "id": "eol-ubuntu",
  "provider": "endoflife",
  "category": "dev-registry",
  "method": "GET",
  "url": "https://endoflife.date/api/ubuntu.json",
  "host": "endoflife.date",
  "path": "/api/ubuntu.json",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 9,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "array",
    "element": {
      "type": "object",
      "keys": {
        "codename": {
          "type": "string"
        },
        "cycle": {
          "type": "string"
        },
        "eol": {
          "type": "string"
        },
        "extendedSupport": {
          "type": "union",
          "variants": [
            "boolean",
            "string"
          ]
        },
        "latest": {
          "type": "string"
        },
        "latestReleaseDate": {
          "type": "string"
        },
        "lts": {
          "type": "boolean"
        },
        "releaseDate": {
          "type": "string"
        },
        "support": {
          "type": "string"
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://endoflife.date/api/ubuntu.json\n// observations  : 95\n// observed      : 2026-07-26 to 2026-08-18\n// fields        : 9 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://endoflife.date/api/ubuntu.json\nexport interface EolUbuntu Array<{\n  codename: string;\n  cycle: string;\n  eol: string;\n  extendedSupport: boolean | string;\n  latest: string;\n  latestReleaseDate: string;\n  lts: boolean;\n  releaseDate: string;\n  support: string;\n}>\n\nimport { z } from \"zod\";\n\nexport const EolUbuntuSchema = z.array(z.object({\n  codename: z.string(),\n  cycle: z.string(),\n  eol: z.string(),\n  extendedSupport: z.union([z.boolean(), z.string()]),\n  latest: z.string(),\n  latestReleaseDate: z.string(),\n  lts: z.boolean(),\n  releaseDate: z.string(),\n  support: z.string(),\n}));\n"
}
