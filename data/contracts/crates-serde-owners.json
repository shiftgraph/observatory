{
  "schema_version": 1,
  "id": "crates-serde-owners",
  "provider": "crates",
  "category": "dev-registry",
  "method": "GET",
  "url": "https://crates.io/api/v1/crates/serde/owners",
  "host": "crates.io",
  "path": "/api/v1/crates/serde/owners",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 8,
  "optional_fields": 1,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "users": {
        "type": "array",
        "element": {
          "type": "object",
          "keys": {
            "avatar": {
              "type": "string"
            },
            "github_username_matches": {
              "type": "boolean",
              "optional": true
            },
            "id": {
              "type": "number"
            },
            "kind": {
              "type": "string"
            },
            "login": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "url": {
              "type": "string"
            }
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://crates.io/api/v1/crates/serde/owners\n// observations  : 144\n// observed      : 2026-07-26 to 2026-09-01\n// fields        : 8 (1 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://crates.io/api/v1/crates/serde/owners\nexport interface CratesSerdeOwners {\n  users: Array<{\n    avatar: string;\n    github_username_matches?: boolean;\n    id: number;\n    kind: string;\n    login: string;\n    name: string;\n    url: string;\n  }>;\n}\n\nimport { z } from \"zod\";\n\nexport const CratesSerdeOwnersSchema = z.object({\n  users: z.array(z.object({\n    avatar: z.string(),\n    github_username_matches: z.boolean().optional(),\n    id: z.number(),\n    kind: z.string(),\n    login: z.string(),\n    name: z.string(),\n    url: z.string(),\n  })),\n});\n"
}
