{
  "schema_version": 1,
  "id": "carbon-intensity",
  "provider": "carbonintensity",
  "category": "energy",
  "method": "GET",
  "url": "https://api.carbonintensity.org.uk/intensity",
  "host": "api.carbonintensity.org.uk",
  "path": "/intensity",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 7,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not",
    "typed null and never seen holding a value: $.data[].intensity.actual"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "data": {
        "type": "array",
        "element": {
          "type": "object",
          "keys": {
            "from": {
              "type": "string"
            },
            "intensity": {
              "type": "object",
              "keys": {
                "actual": {
                  "type": "null"
                },
                "forecast": {
                  "type": "number"
                },
                "index": {
                  "type": "string"
                }
              }
            },
            "to": {
              "type": "string"
            }
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.carbonintensity.org.uk/intensity\n// observations  : 30\n// observed      : 2026-07-26 to 2026-08-02\n// fields        : 7 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n// NOTE: typed null and never seen holding a value: $.data[].intensity.actual\n// NOTE: 1 field was observed ONLY as null, so it is typed `null` and cannot hold a value. That is what was seen, not necessarily the contract: widen to `T | null` if you know the type. $.data[].intensity.actual\n//\n// Regenerate: npx @shiftgraph/generate https://api.carbonintensity.org.uk/intensity\nexport interface CarbonIntensity {\n  data: Array<{\n    from: string;\n    intensity: {\n      actual: null;\n      forecast: number;\n      index: string;\n    };\n    to: string;\n  }>;\n}\n\nimport { z } from \"zod\";\n\nexport const CarbonIntensitySchema = z.object({\n  data: z.array(z.object({\n    from: z.string(),\n    intensity: z.object({\n      actual: z.null(),\n      forecast: z.number(),\n      index: z.string(),\n    }),\n    to: z.string(),\n  })),\n});\n"
}
