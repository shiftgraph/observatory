{
  "schema_version": 1,
  "id": "restcountries-japan",
  "provider": "restcountries",
  "category": "reference",
  "method": "GET",
  "url": "https://restcountries.com/v3.1/name/japan",
  "host": "restcountries.com",
  "path": "/v3.1/name/japan",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 4,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not",
    "typed null and never seen holding a value: $.data"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "data": {
        "type": "null"
      },
      "errors": {
        "type": "array",
        "element": {
          "type": "object",
          "keys": {
            "message": {
              "type": "string"
            }
          }
        }
      },
      "success": {
        "type": "boolean"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://restcountries.com/v3.1/name/japan\n// observations  : 149\n// observed      : 2026-07-26 to 2026-09-02\n// fields        : 4 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n// NOTE: typed null and never seen holding a value: $.data\n// NOTE: 1 field was observed ONLY as null, so it is typed `null` and cannot hold a value. That is what was seen, not necessarily the contract: widen to `T | null` if you know the type. $.data\n//\n// Regenerate: npx @shiftgraph/generate https://restcountries.com/v3.1/name/japan\nexport interface RestcountriesJapan {\n  data: null;\n  errors: Array<{\n    message: string;\n  }>;\n  success: boolean;\n}\n\nimport { z } from \"zod\";\n\nexport const RestcountriesJapanSchema = z.object({\n  data: z.null(),\n  errors: z.array(z.object({\n    message: z.string(),\n  })),\n  success: z.boolean(),\n});\n"
}
