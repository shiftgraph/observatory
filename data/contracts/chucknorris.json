{
  "schema_version": 1,
  "id": "chucknorris",
  "provider": "chucknorris",
  "category": "sample",
  "method": "GET",
  "url": "https://api.chucknorris.io/jokes/random",
  "host": "api.chucknorris.io",
  "path": "/jokes/random",
  "observed_from": "2026-07-24",
  "confidence": "strong",
  "fields": 7,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "categories": {
        "type": "array",
        "element": {
          "type": "string"
        }
      },
      "created_at": {
        "type": "string"
      },
      "icon_url": {
        "type": "string"
      },
      "id": {
        "type": "string"
      },
      "updated_at": {
        "type": "string"
      },
      "url": {
        "type": "string"
      },
      "value": {
        "type": "string"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.chucknorris.io/jokes/random\n// observations  : 29\n// observed      : 2026-07-24 to 2026-08-01\n// fields        : 7 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.chucknorris.io/jokes/random\nexport interface Chucknorris {\n  categories: Array<string>;\n  created_at: string;\n  icon_url: string;\n  id: string;\n  updated_at: string;\n  url: string;\n  value: string;\n}\n\nimport { z } from \"zod\";\n\nexport const ChucknorrisSchema = z.object({\n  categories: z.array(z.string()),\n  created_at: z.string(),\n  icon_url: z.string(),\n  id: z.string(),\n  updated_at: z.string(),\n  url: z.string(),\n  value: z.string(),\n});\n"
}
