{
  "schema_version": 1,
  "id": "httpbin-json",
  "provider": "httpbin",
  "category": "sample",
  "method": "GET",
  "url": "https://httpbin.org/json",
  "host": "httpbin.org",
  "path": "/json",
  "observed_from": "2026-07-24",
  "confidence": "strong",
  "fields": 8,
  "optional_fields": 1,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "slideshow": {
        "type": "object",
        "keys": {
          "author": {
            "type": "string"
          },
          "date": {
            "type": "string"
          },
          "slides": {
            "type": "array",
            "element": {
              "type": "object",
              "keys": {
                "items": {
                  "type": "array",
                  "element": {
                    "type": "string"
                  },
                  "optional": true
                },
                "title": {
                  "type": "string"
                },
                "type": {
                  "type": "string"
                }
              }
            }
          },
          "title": {
            "type": "string"
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://httpbin.org/json\n// observations  : 64\n// observed      : 2026-07-24 to 2026-08-21\n// fields        : 8 (1 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://httpbin.org/json\nexport interface HttpbinJson {\n  slideshow: {\n    author: string;\n    date: string;\n    slides: Array<{\n      items?: Array<string>;\n      title: string;\n      type: string;\n    }>;\n    title: string;\n  };\n}\n\nimport { z } from \"zod\";\n\nexport const HttpbinJsonSchema = z.object({\n  slideshow: z.object({\n    author: z.string(),\n    date: z.string(),\n    slides: z.array(z.object({\n      items: z.array(z.string()).optional(),\n      title: z.string(),\n      type: z.string(),\n    })),\n    title: z.string(),\n  }),\n});\n"
}
