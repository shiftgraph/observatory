{
  "schema_version": 1,
  "id": "coingecko-price",
  "provider": "coingecko",
  "category": "crypto",
  "method": "GET",
  "url": "https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd",
  "host": "api.coingecko.com",
  "path": "/api/v3/simple/price",
  "observed_from": "2026-07-24",
  "confidence": "strong",
  "fields": 4,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "bitcoin": {
        "type": "object",
        "keys": {
          "usd": {
            "type": "number"
          }
        }
      },
      "ethereum": {
        "type": "object",
        "keys": {
          "usd": {
            "type": "number"
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd\n// observations  : 89\n// observed      : 2026-07-24 to 2026-08-16\n// fields        : 4 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&vs_currencies=usd\nexport interface CoingeckoPrice {\n  bitcoin: {\n    usd: number;\n  };\n  ethereum: {\n    usd: number;\n  };\n}\n\nimport { z } from \"zod\";\n\nexport const CoingeckoPriceSchema = z.object({\n  bitcoin: z.object({\n    usd: z.number(),\n  }),\n  ethereum: z.object({\n    usd: z.number(),\n  }),\n});\n"
}
