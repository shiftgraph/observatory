{
  "schema_version": 1,
  "id": "eol-python",
  "provider": "endoflife",
  "category": "dev-registry",
  "method": "GET",
  "url": "https://endoflife.date/api/python.json",
  "host": "endoflife.date",
  "path": "/api/python.json",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 8,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "array",
    "element": {
      "type": "object",
      "keys": {
        "cycle": {
          "type": "string"
        },
        "eol": {
          "type": "string"
        },
        "latest": {
          "type": "string"
        },
        "latestReleaseDate": {
          "type": "string"
        },
        "lts": {
          "type": "boolean"
        },
        "pep": {
          "type": "string"
        },
        "releaseDate": {
          "type": "string"
        },
        "support": {
          "type": "union",
          "variants": [
            "boolean",
            "string"
          ]
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://endoflife.date/api/python.json\n// observations  : 57\n// observed      : 2026-07-26 to 2026-08-09\n// fields        : 8 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://endoflife.date/api/python.json\nexport interface EolPython Array<{\n  cycle: string;\n  eol: string;\n  latest: string;\n  latestReleaseDate: string;\n  lts: boolean;\n  pep: string;\n  releaseDate: string;\n  support: boolean | string;\n}>\n\nimport { z } from \"zod\";\n\nexport const EolPythonSchema = z.array(z.object({\n  cycle: z.string(),\n  eol: z.string(),\n  latest: z.string(),\n  latestReleaseDate: z.string(),\n  lts: z.boolean(),\n  pep: z.string(),\n  releaseDate: z.string(),\n  support: z.union([z.boolean(), z.string()]),\n}));\n"
}
