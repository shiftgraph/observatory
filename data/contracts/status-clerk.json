{
  "schema_version": 1,
  "id": "status-clerk",
  "provider": "clerk",
  "category": "status-page",
  "method": "GET",
  "url": "https://status.clerk.com/api/v2/summary.json",
  "host": "status.clerk.com",
  "path": "/api/v2/summary.json",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 16,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "components": {
        "type": "array",
        "element": {
          "type": "object",
          "keys": {
            "created_at": {
              "type": "string"
            },
            "id": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "page_id": {
              "type": "string"
            },
            "position": {
              "type": "number"
            },
            "status": {
              "type": "string"
            },
            "updated_at": {
              "type": "string"
            }
          }
        }
      },
      "page": {
        "type": "object",
        "keys": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "updated_at": {
            "type": "string"
          },
          "url": {
            "type": "string"
          }
        }
      },
      "status": {
        "type": "object",
        "keys": {
          "description": {
            "type": "string"
          },
          "indicator": {
            "type": "string"
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://status.clerk.com/api/v2/summary.json\n// observations  : 114\n// observed      : 2026-07-26 to 2026-08-23\n// fields        : 16 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://status.clerk.com/api/v2/summary.json\nexport interface StatusClerk {\n  components: Array<{\n    created_at: string;\n    id: string;\n    name: string;\n    page_id: string;\n    position: number;\n    status: string;\n    updated_at: string;\n  }>;\n  page: {\n    id: string;\n    name: string;\n    updated_at: string;\n    url: string;\n  };\n  status: {\n    description: string;\n    indicator: string;\n  };\n}\n\nimport { z } from \"zod\";\n\nexport const StatusClerkSchema = z.object({\n  components: z.array(z.object({\n    created_at: z.string(),\n    id: z.string(),\n    name: z.string(),\n    page_id: z.string(),\n    position: z.number(),\n    status: z.string(),\n    updated_at: z.string(),\n  })),\n  page: z.object({\n    id: z.string(),\n    name: z.string(),\n    updated_at: z.string(),\n    url: z.string(),\n  }),\n  status: z.object({\n    description: z.string(),\n    indicator: z.string(),\n  }),\n});\n"
}
