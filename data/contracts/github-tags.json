{
  "schema_version": 1,
  "id": "github-tags",
  "provider": "github",
  "category": "dev-platform",
  "method": "GET",
  "url": "https://api.github.com/repos/facebook/react/tags?per_page=3",
  "host": "api.github.com",
  "path": "/repos/facebook/react/tags",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 7,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "array",
    "element": {
      "type": "object",
      "keys": {
        "commit": {
          "type": "object",
          "keys": {
            "sha": {
              "type": "string"
            },
            "url": {
              "type": "string"
            }
          }
        },
        "name": {
          "type": "string"
        },
        "node_id": {
          "type": "string"
        },
        "tarball_url": {
          "type": "string"
        },
        "zipball_url": {
          "type": "string"
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.github.com/repos/facebook/react/tags?per_page=3\n// observations  : 15\n// observed      : 2026-07-26 to 2026-07-29\n// fields        : 7 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.github.com/repos/facebook/react/tags?per_page=3\nexport interface GithubTags Array<{\n  commit: {\n    sha: string;\n    url: string;\n  };\n  name: string;\n  node_id: string;\n  tarball_url: string;\n  zipball_url: string;\n}>\n\nimport { z } from \"zod\";\n\nexport const GithubTagsSchema = z.array(z.object({\n  commit: z.object({\n    sha: z.string(),\n    url: z.string(),\n  }),\n  name: z.string(),\n  node_id: z.string(),\n  tarball_url: z.string(),\n  zipball_url: z.string(),\n}));\n"
}
