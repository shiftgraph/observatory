{
  "schema_version": 1,
  "id": "coinbase-time",
  "provider": "coinbase",
  "category": "crypto",
  "method": "GET",
  "url": "https://api.coinbase.com/v2/time",
  "host": "api.coinbase.com",
  "path": "/v2/time",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 3,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "data": {
        "type": "object",
        "keys": {
          "epoch": {
            "type": "number"
          },
          "iso": {
            "type": "string"
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.coinbase.com/v2/time\n// observations  : 43\n// observed      : 2026-07-26 to 2026-08-05\n// fields        : 3 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.coinbase.com/v2/time\nexport interface CoinbaseTime {\n  data: {\n    epoch: number;\n    iso: string;\n  };\n}\n\nimport { z } from \"zod\";\n\nexport const CoinbaseTimeSchema = z.object({\n  data: z.object({\n    epoch: z.number(),\n    iso: z.string(),\n  }),\n});\n"
}
