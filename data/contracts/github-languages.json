{
  "schema_version": 1,
  "id": "github-languages",
  "provider": "github",
  "category": "dev-platform",
  "method": "GET",
  "url": "https://api.github.com/repos/facebook/react/languages",
  "host": "api.github.com",
  "path": "/repos/facebook/react/languages",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 7,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "CSS": {
        "type": "number"
      },
      "CoffeeScript": {
        "type": "number"
      },
      "HTML": {
        "type": "number"
      },
      "JavaScript": {
        "type": "number"
      },
      "Rust": {
        "type": "number"
      },
      "Shell": {
        "type": "number"
      },
      "TypeScript": {
        "type": "number"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.github.com/repos/facebook/react/languages\n// observations  : 70\n// observed      : 2026-07-26 to 2026-08-12\n// fields        : 7 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.github.com/repos/facebook/react/languages\nexport interface GithubLanguages {\n  CSS: number;\n  CoffeeScript: number;\n  HTML: number;\n  JavaScript: number;\n  Rust: number;\n  Shell: number;\n  TypeScript: number;\n}\n\nimport { z } from \"zod\";\n\nexport const GithubLanguagesSchema = z.object({\n  CSS: z.number(),\n  CoffeeScript: z.number(),\n  HTML: z.number(),\n  JavaScript: z.number(),\n  Rust: z.number(),\n  Shell: z.number(),\n  TypeScript: z.number(),\n});\n"
}
