{
  "schema_version": 1,
  "id": "kraken-time",
  "provider": "kraken",
  "category": "crypto",
  "method": "GET",
  "url": "https://api.kraken.com/0/public/Time",
  "host": "api.kraken.com",
  "path": "/0/public/Time",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 4,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "error": {
        "type": "array",
        "element": {
          "type": "unknown-empty-array"
        }
      },
      "result": {
        "type": "object",
        "keys": {
          "rfc1123": {
            "type": "string"
          },
          "unixtime": {
            "type": "number"
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.kraken.com/0/public/Time\n// observations  : 121\n// observed      : 2026-07-26 to 2026-08-25\n// fields        : 4 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.kraken.com/0/public/Time\nexport interface KrakenTime {\n  error: Array<unknown>;\n  result: {\n    rfc1123: string;\n    unixtime: number;\n  };\n}\n\nimport { z } from \"zod\";\n\nexport const KrakenTimeSchema = z.object({\n  error: z.array(z.unknown()),\n  result: z.object({\n    rfc1123: z.string(),\n    unixtime: z.number(),\n  }),\n});\n"
}
