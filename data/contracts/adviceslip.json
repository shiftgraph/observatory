{
  "schema_version": 1,
  "id": "adviceslip",
  "provider": "adviceslip",
  "category": "sample",
  "method": "GET",
  "url": "https://api.adviceslip.com/advice",
  "host": "api.adviceslip.com",
  "path": "/advice",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 3,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "slip": {
        "type": "object",
        "keys": {
          "advice": {
            "type": "string"
          },
          "id": {
            "type": "number"
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.adviceslip.com/advice\n// observations  : 74\n// observed      : 2026-07-26 to 2026-08-16\n// fields        : 3 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.adviceslip.com/advice\nexport interface Adviceslip {\n  slip: {\n    advice: string;\n    id: number;\n  };\n}\n\nimport { z } from \"zod\";\n\nexport const AdviceslipSchema = z.object({\n  slip: z.object({\n    advice: z.string(),\n    id: z.number(),\n  }),\n});\n"
}
