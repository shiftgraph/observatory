{
  "schema_version": 1,
  "id": "httpbin-get",
  "provider": "httpbin",
  "category": "sample",
  "method": "GET",
  "url": "https://httpbin.org/get",
  "host": "httpbin.org",
  "path": "/get",
  "observed_from": "2026-07-24",
  "confidence": "strong",
  "fields": 11,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "args": {
        "type": "object",
        "keys": {}
      },
      "headers": {
        "type": "object",
        "keys": {
          "Accept": {
            "type": "string"
          },
          "Accept-Encoding": {
            "type": "string"
          },
          "Accept-Language": {
            "type": "string"
          },
          "Host": {
            "type": "string"
          },
          "Sec-Fetch-Mode": {
            "type": "string"
          },
          "User-Agent": {
            "type": "string"
          },
          "X-Amzn-Trace-Id": {
            "type": "string"
          }
        }
      },
      "origin": {
        "type": "string"
      },
      "url": {
        "type": "string"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://httpbin.org/get\n// observations  : 48\n// observed      : 2026-07-24 to 2026-08-15\n// fields        : 11 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://httpbin.org/get\nexport interface HttpbinGet {\n  args: Record<string, unknown>;\n  headers: {\n    Accept: string;\n    \"Accept-Encoding\": string;\n    \"Accept-Language\": string;\n    Host: string;\n    \"Sec-Fetch-Mode\": string;\n    \"User-Agent\": string;\n    \"X-Amzn-Trace-Id\": string;\n  };\n  origin: string;\n  url: string;\n}\n\nimport { z } from \"zod\";\n\nexport const HttpbinGetSchema = z.object({\n  args: z.record(z.unknown()),\n  headers: z.object({\n    Accept: z.string(),\n    \"Accept-Encoding\": z.string(),\n    \"Accept-Language\": z.string(),\n    Host: z.string(),\n    \"Sec-Fetch-Mode\": z.string(),\n    \"User-Agent\": z.string(),\n    \"X-Amzn-Trace-Id\": z.string(),\n  }),\n  origin: z.string(),\n  url: z.string(),\n});\n"
}
