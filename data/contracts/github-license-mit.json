{
  "schema_version": 1,
  "id": "github-license-mit",
  "provider": "github",
  "category": "dev-platform",
  "method": "GET",
  "url": "https://api.github.com/licenses/mit",
  "host": "api.github.com",
  "path": "/licenses/mit",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 13,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "body": {
        "type": "string"
      },
      "conditions": {
        "type": "array",
        "element": {
          "type": "string"
        }
      },
      "description": {
        "type": "string"
      },
      "featured": {
        "type": "boolean"
      },
      "html_url": {
        "type": "string"
      },
      "implementation": {
        "type": "string"
      },
      "key": {
        "type": "string"
      },
      "limitations": {
        "type": "array",
        "element": {
          "type": "string"
        }
      },
      "name": {
        "type": "string"
      },
      "node_id": {
        "type": "string"
      },
      "permissions": {
        "type": "array",
        "element": {
          "type": "string"
        }
      },
      "spdx_id": {
        "type": "string"
      },
      "url": {
        "type": "string"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.github.com/licenses/mit\n// observations  : 87\n// observed      : 2026-07-26 to 2026-08-17\n// fields        : 13 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.github.com/licenses/mit\nexport interface GithubLicenseMit {\n  body: string;\n  conditions: Array<string>;\n  description: string;\n  featured: boolean;\n  html_url: string;\n  implementation: string;\n  key: string;\n  limitations: Array<string>;\n  name: string;\n  node_id: string;\n  permissions: Array<string>;\n  spdx_id: string;\n  url: string;\n}\n\nimport { z } from \"zod\";\n\nexport const GithubLicenseMitSchema = z.object({\n  body: z.string(),\n  conditions: z.array(z.string()),\n  description: z.string(),\n  featured: z.boolean(),\n  html_url: z.string(),\n  implementation: z.string(),\n  key: z.string(),\n  limitations: z.array(z.string()),\n  name: z.string(),\n  node_id: z.string(),\n  permissions: z.array(z.string()),\n  spdx_id: z.string(),\n  url: z.string(),\n});\n"
}
