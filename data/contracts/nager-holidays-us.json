{
  "schema_version": 1,
  "id": "nager-holidays-us",
  "provider": "nager",
  "category": "reference",
  "method": "GET",
  "url": "https://date.nager.at/api/v3/PublicHolidays/2026/US",
  "host": "date.nager.at",
  "path": "/api/v3/PublicHolidays/2026/US",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 9,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not",
    "typed null and never seen holding a value: $[].launchYear"
  ],
  "profile": {
    "type": "array",
    "element": {
      "type": "object",
      "keys": {
        "counties": {
          "type": "array",
          "element": {
            "type": "string"
          }
        },
        "countryCode": {
          "type": "string"
        },
        "date": {
          "type": "string"
        },
        "fixed": {
          "type": "boolean"
        },
        "global": {
          "type": "boolean"
        },
        "launchYear": {
          "type": "null"
        },
        "localName": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "types": {
          "type": "array",
          "element": {
            "type": "string"
          }
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://date.nager.at/api/v3/PublicHolidays/2026/US\n// observations  : 83\n// observed      : 2026-07-26 to 2026-08-15\n// fields        : 9 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n// NOTE: typed null and never seen holding a value: $[].launchYear\n// NOTE: 1 field was observed ONLY as null, so it is typed `null` and cannot hold a value. That is what was seen, not necessarily the contract: widen to `T | null` if you know the type. $[].launchYear\n//\n// Regenerate: npx @shiftgraph/generate https://date.nager.at/api/v3/PublicHolidays/2026/US\nexport interface NagerHolidaysUs Array<{\n  counties: Array<string>;\n  countryCode: string;\n  date: string;\n  fixed: boolean;\n  global: boolean;\n  launchYear: null;\n  localName: string;\n  name: string;\n  types: Array<string>;\n}>\n\nimport { z } from \"zod\";\n\nexport const NagerHolidaysUsSchema = z.array(z.object({\n  counties: z.array(z.string()),\n  countryCode: z.string(),\n  date: z.string(),\n  fixed: z.boolean(),\n  global: z.boolean(),\n  launchYear: z.null(),\n  localName: z.string(),\n  name: z.string(),\n  types: z.array(z.string()),\n}));\n"
}
