{
  "schema_version": 1,
  "id": "sunrise-sunset",
  "provider": "sunrise-sunset",
  "category": "reference",
  "method": "GET",
  "url": "https://api.sunrise-sunset.org/json?lat=36.7201600&lng=-4.4203400",
  "host": "api.sunrise-sunset.org",
  "path": "/json",
  "observed_from": "2026-07-26",
  "confidence": "thin",
  "fields": 13,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "results": {
        "type": "object",
        "keys": {
          "astronomical_twilight_begin": {
            "type": "string"
          },
          "astronomical_twilight_end": {
            "type": "string"
          },
          "civil_twilight_begin": {
            "type": "string"
          },
          "civil_twilight_end": {
            "type": "string"
          },
          "day_length": {
            "type": "string"
          },
          "nautical_twilight_begin": {
            "type": "string"
          },
          "nautical_twilight_end": {
            "type": "string"
          },
          "solar_noon": {
            "type": "string"
          },
          "sunrise": {
            "type": "string"
          },
          "sunset": {
            "type": "string"
          }
        }
      },
      "status": {
        "type": "string"
      },
      "tzid": {
        "type": "string"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.sunrise-sunset.org/json?lat=36.7201600&lng=-4.4203400\n// observations  : 9\n// observed      : 2026-07-26 to 2026-07-28\n// fields        : 13 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.sunrise-sunset.org/json?lat=36.7201600&lng=-4.4203400\nexport interface SunriseSunset {\n  results: {\n    astronomical_twilight_begin: string;\n    astronomical_twilight_end: string;\n    civil_twilight_begin: string;\n    civil_twilight_end: string;\n    day_length: string;\n    nautical_twilight_begin: string;\n    nautical_twilight_end: string;\n    solar_noon: string;\n    sunrise: string;\n    sunset: string;\n  };\n  status: string;\n  tzid: string;\n}\n\nimport { z } from \"zod\";\n\nexport const SunriseSunsetSchema = z.object({\n  results: z.object({\n    astronomical_twilight_begin: z.string(),\n    astronomical_twilight_end: z.string(),\n    civil_twilight_begin: z.string(),\n    civil_twilight_end: z.string(),\n    day_length: z.string(),\n    nautical_twilight_begin: z.string(),\n    nautical_twilight_end: z.string(),\n    solar_noon: z.string(),\n    sunrise: z.string(),\n    sunset: z.string(),\n  }),\n  status: z.string(),\n  tzid: z.string(),\n});\n"
}
