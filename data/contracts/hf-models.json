{
  "schema_version": 1,
  "id": "hf-models",
  "provider": "huggingface",
  "category": "model-hub",
  "method": "GET",
  "url": "https://huggingface.co/api/models?limit=3",
  "host": "huggingface.co",
  "path": "/api/models",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 11,
  "optional_fields": 2,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "array",
    "element": {
      "type": "object",
      "keys": {
        "_id": {
          "type": "string"
        },
        "createdAt": {
          "type": "string"
        },
        "downloads": {
          "type": "number"
        },
        "id": {
          "type": "string"
        },
        "library_name": {
          "type": "string",
          "optional": true
        },
        "likes": {
          "type": "number"
        },
        "modelId": {
          "type": "string"
        },
        "pipeline_tag": {
          "type": "string",
          "optional": true
        },
        "private": {
          "type": "boolean"
        },
        "tags": {
          "type": "array",
          "element": {
            "type": "string"
          }
        },
        "trendingScore": {
          "type": "number"
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://huggingface.co/api/models?limit=3\n// observations  : 158\n// observed      : 2026-07-26 to 2026-09-04\n// fields        : 11 (2 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://huggingface.co/api/models?limit=3\nexport interface HfModels Array<{\n  _id: string;\n  createdAt: string;\n  downloads: number;\n  id: string;\n  library_name?: string;\n  likes: number;\n  modelId: string;\n  pipeline_tag?: string;\n  private: boolean;\n  tags: Array<string>;\n  trendingScore: number;\n}>\n\nimport { z } from \"zod\";\n\nexport const HfModelsSchema = z.array(z.object({\n  _id: z.string(),\n  createdAt: z.string(),\n  downloads: z.number(),\n  id: z.string(),\n  library_name: z.string().optional(),\n  likes: z.number(),\n  modelId: z.string(),\n  pipeline_tag: z.string().optional(),\n  private: z.boolean(),\n  tags: z.array(z.string()),\n  trendingScore: z.number(),\n}));\n"
}
