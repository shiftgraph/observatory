{
  "schema_version": 1,
  "id": "wikimedia-bare",
  "provider": "wikimedia",
  "category": "reference",
  "method": "GET",
  "url": "https://api.wikimedia.org/core/v1/wikipedia/en/page/Earth/bare",
  "host": "api.wikimedia.org",
  "path": "/core/v1/wikipedia/en/page/Earth/bare",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 11,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "content_model": {
        "type": "string"
      },
      "html_url": {
        "type": "string"
      },
      "id": {
        "type": "number"
      },
      "key": {
        "type": "string"
      },
      "latest": {
        "type": "object",
        "keys": {
          "id": {
            "type": "number"
          },
          "timestamp": {
            "type": "string"
          }
        }
      },
      "license": {
        "type": "object",
        "keys": {
          "title": {
            "type": "string"
          },
          "url": {
            "type": "string"
          }
        }
      },
      "title": {
        "type": "string"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.wikimedia.org/core/v1/wikipedia/en/page/Earth/bare\n// observations  : 75\n// observed      : 2026-07-26 to 2026-08-13\n// fields        : 11 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.wikimedia.org/core/v1/wikipedia/en/page/Earth/bare\nexport interface WikimediaBare {\n  content_model: string;\n  html_url: string;\n  id: number;\n  key: string;\n  latest: {\n    id: number;\n    timestamp: string;\n  };\n  license: {\n    title: string;\n    url: string;\n  };\n  title: string;\n}\n\nimport { z } from \"zod\";\n\nexport const WikimediaBareSchema = z.object({\n  content_model: z.string(),\n  html_url: z.string(),\n  id: z.number(),\n  key: z.string(),\n  latest: z.object({\n    id: z.number(),\n    timestamp: z.string(),\n  }),\n  license: z.object({\n    title: z.string(),\n    url: z.string(),\n  }),\n  title: z.string(),\n});\n"
}
