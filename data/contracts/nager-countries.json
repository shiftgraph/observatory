{
  "schema_version": 1,
  "id": "nager-countries",
  "provider": "nager",
  "category": "reference",
  "method": "GET",
  "url": "https://date.nager.at/api/v3/AvailableCountries",
  "host": "date.nager.at",
  "path": "/api/v3/AvailableCountries",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 2,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "array",
    "element": {
      "type": "object",
      "keys": {
        "countryCode": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://date.nager.at/api/v3/AvailableCountries\n// observations  : 32\n// observed      : 2026-07-26 to 2026-08-02\n// fields        : 2 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://date.nager.at/api/v3/AvailableCountries\nexport interface NagerCountries Array<{\n  countryCode: string;\n  name: string;\n}>\n\nimport { z } from \"zod\";\n\nexport const NagerCountriesSchema = z.array(z.object({\n  countryCode: z.string(),\n  name: z.string(),\n}));\n"
}
