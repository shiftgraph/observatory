{
  "schema_version": 1,
  "id": "eol-postgresql",
  "provider": "endoflife",
  "category": "dev-registry",
  "method": "GET",
  "url": "https://endoflife.date/api/postgresql.json",
  "host": "endoflife.date",
  "path": "/api/postgresql.json",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 6,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "array",
    "element": {
      "type": "object",
      "keys": {
        "cycle": {
          "type": "string"
        },
        "eol": {
          "type": "string"
        },
        "latest": {
          "type": "string"
        },
        "latestReleaseDate": {
          "type": "string"
        },
        "lts": {
          "type": "boolean"
        },
        "releaseDate": {
          "type": "string"
        }
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://endoflife.date/api/postgresql.json\n// observations  : 159\n// observed      : 2026-07-26 to 2026-09-05\n// fields        : 6 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://endoflife.date/api/postgresql.json\nexport interface EolPostgresql Array<{\n  cycle: string;\n  eol: string;\n  latest: string;\n  latestReleaseDate: string;\n  lts: boolean;\n  releaseDate: string;\n}>\n\nimport { z } from \"zod\";\n\nexport const EolPostgresqlSchema = z.array(z.object({\n  cycle: z.string(),\n  eol: z.string(),\n  latest: z.string(),\n  latestReleaseDate: z.string(),\n  lts: z.boolean(),\n  releaseDate: z.string(),\n}));\n"
}
