{
  "schema_version": 1,
  "id": "ipapi-json",
  "provider": "ip-api",
  "category": "geo",
  "method": "GET",
  "url": "http://ip-api.com/json",
  "host": "ip-api.com",
  "path": "/json",
  "observed_from": "2026-07-24",
  "confidence": "strong",
  "fields": 14,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "as": {
        "type": "string"
      },
      "city": {
        "type": "string"
      },
      "country": {
        "type": "string"
      },
      "countryCode": {
        "type": "string"
      },
      "isp": {
        "type": "string"
      },
      "lat": {
        "type": "number"
      },
      "lon": {
        "type": "number"
      },
      "org": {
        "type": "string"
      },
      "query": {
        "type": "string"
      },
      "region": {
        "type": "string"
      },
      "regionName": {
        "type": "string"
      },
      "status": {
        "type": "string"
      },
      "timezone": {
        "type": "string"
      },
      "zip": {
        "type": "string"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET http://ip-api.com/json\n// observations  : 120\n// observed      : 2026-07-24 to 2026-08-24\n// fields        : 14 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate http://ip-api.com/json\nexport interface IpapiJson {\n  as: string;\n  city: string;\n  country: string;\n  countryCode: string;\n  isp: string;\n  lat: number;\n  lon: number;\n  org: string;\n  query: string;\n  region: string;\n  regionName: string;\n  status: string;\n  timezone: string;\n  zip: string;\n}\n\nimport { z } from \"zod\";\n\nexport const IpapiJsonSchema = z.object({\n  as: z.string(),\n  city: z.string(),\n  country: z.string(),\n  countryCode: z.string(),\n  isp: z.string(),\n  lat: z.number(),\n  lon: z.number(),\n  org: z.string(),\n  query: z.string(),\n  region: z.string(),\n  regionName: z.string(),\n  status: z.string(),\n  timezone: z.string(),\n  zip: z.string(),\n});\n"
}
