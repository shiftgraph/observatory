{
  "schema_version": 1,
  "id": "nuget-index",
  "provider": "nuget",
  "category": "dev-registry",
  "method": "GET",
  "url": "https://api.nuget.org/v3/index.json",
  "host": "api.nuget.org",
  "path": "/v3/index.json",
  "observed_from": "2026-07-26",
  "confidence": "strong",
  "fields": 5,
  "optional_fields": 0,
  "oversize": false,
  "notes": [
    "observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not"
  ],
  "profile": {
    "type": "object",
    "keys": {
      "@context": {
        "type": "object",
        "keys": {
          "{key}": {
            "type": "string"
          }
        }
      },
      "resources": {
        "type": "array",
        "element": {
          "type": "object",
          "keys": {
            "{key}": {
              "type": "string"
            }
          }
        }
      },
      "version": {
        "type": "string"
      }
    }
  },
  "typescript": "// Generated by ShiftGraph from OBSERVED responses. Do not edit by hand.\n//\n// source        : GET https://api.nuget.org/v3/index.json\n// observations  : 13\n// observed      : 2026-07-26 to 2026-07-29\n// fields        : 5 (0 optional)\n//\n// Optionality here is earned, not assumed: a field is marked optional only\n// where this interface was actually watched omitting it. A field never seen\n// absent is required, because that is what was observed. A type generated\n// from a specification would mark nearly all of these optional, which is\n// why one of those is useful at a call site and the other is not.\n//\n// NOTE: observed on public, unauthenticated requests. A response to an authenticated caller, on a different plan, or for a different resource can carry fields this does not\n//\n// Regenerate: npx @shiftgraph/generate https://api.nuget.org/v3/index.json\nexport interface NugetIndex {\n  \"@context\": Record<string, string>;\n  resources: Array<Record<string, string>>;\n  version: string;\n}\n\nimport { z } from \"zod\";\n\nexport const NugetIndexSchema = z.object({\n  \"@context\": z.record(z.string()),\n  resources: z.array(z.record(z.string())),\n  version: z.string(),\n});\n"
}
